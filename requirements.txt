# EnsinaLab Content Engine - Requirements
# Updated: 2025-11-09 - Added authentication dependencies

# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
# Pydantic: require a newer 2.x release compatible with LangChain 1.0.x
pydantic>=2.7.4,<3.0.0
# Pydantic-settings matching pydantic 2.x
pydantic-settings>=2.1.0,<3.0.0

# Authentication
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Celery (async tasks)
celery==5.3.4
redis==5.0.1

# OpenAI (compatível com Python 3.9+)
openai>=1.0.0,<2.0.0

# LangChain + LangGraph - versões 0.3.x compatíveis com Python 3.9
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langgraph>=0.2.0,<0.3.0
langsmith>=0.1.0,<0.2.0

# Vector DB (escolher uma)
# qdrant-client==1.7.0
# pinecone-client==2.2.4

# Video generation
moviepy==1.0.3
# Pin Pillow to a release compatible with MoviePy and Python 3.9+
Pillow>=10.0.0,<12.0.0
# Pin numpy for Python 3.9 compatibility
numpy>=1.24.0,<1.27.0

# TTS Multi-provider Support
google-cloud-texttospeech>=2.14.1,<3.0.0  # Google Cloud TTS
elevenlabs>=0.2.27,<1.0.0  # ElevenLabs TTS
boto3>=1.29.7,<2.0.0  # Amazon Polly
azure-cognitiveservices-speech>=1.31.0,<2.0.0  # Azure Speech
pydub>=0.25.1,<1.0.0  # Audio manipulation (fallback generation)

# Utils
python-dotenv==1.0.0
python-multipart==0.0.6
httpx==0.25.2
requests>=2.31.0,<3.0.0  # Para download de vídeos (HeyGen, D-ID)
