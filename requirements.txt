# EnsinaLab Content Engine - Requirements
# Updated: 2025-11-07 - Fixed dependency conflicts

# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
# Pydantic: require a newer 2.x release compatible with LangChain 1.0.x
pydantic>=2.7.4,<3.0.0
# Pydantic-settings matching pydantic 2.x
pydantic-settings>=2.1.0,<3.0.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Celery (async tasks)
celery==5.3.4
redis==5.0.1

# OpenAI (versão testada localmente)
openai==1.109.1

# LangChain + LangGraph - pinned to tested compatible versions to avoid resolver backtracking
langchain==1.0.3
langchain-core==1.0.3
langchain-community==0.4.1
langchain-openai==1.0.2
langgraph==1.0.2
langsmith==0.3.45

# Vector DB (escolher uma)
# qdrant-client==1.7.0
# pinecone-client==2.2.4

# Video generation
moviepy==1.0.3
# Pin Pillow to a modern known-good release to simplify resolver
Pillow==11.3.0
# Pin numpy exactly to avoid resolver downloads/backtracking
numpy==1.26.4

# TTS (escolher uma opção)
# elevenlabs==0.2.27  # ElevenLabs TTS
# boto3==1.29.7  # Amazon Polly
# TTS==0.20.0  # Coqui TTS (open-source)

# Utils
python-dotenv==1.0.0
python-multipart==0.0.6
httpx==0.25.2
